server:
  port: 7080

spring:
  datasource:
    username: root
    password: tian1013
    url: jdbc:mysql://localhost:3306/springboot-seed?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com:
      tyron:
        mapper: debug